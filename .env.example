DB_USERNAME=
DB_PASSWORD=
GOOGLE_API_KEY=
OPENWEATHER_API_KEY=
IGNAV_API_KEY=
TRAVEL_PLANNER_JWT_SECRET=
TRAVEL_PLANNER_JWT_EXPIRATION_MS=
ADMIN_PASSWORD=

# Comma-separated list of origins allowed to call the API from a browser.
# Locally: http://localhost:5173 (Vite) or http://localhost:3000 (Compose).
# On Render: the deployed frontend's address.
APP_CORS_ALLOWED_ORIGINS=